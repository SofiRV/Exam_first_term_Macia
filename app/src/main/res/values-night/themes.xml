<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Base.Theme.ExamMaciaFirstTerm" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/barbie_pink</item>
        <item name="colorPrimaryDark">@color/barbie_pink_dark</item>
        <item name="colorAccent">@color/barbie_pink</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/barbie_pink_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.ExamMaciaFirstTerm" parent="Base.Theme.ExamMaciaFirstTerm" />
</resources>